/**
 * Define events used by web components
 * @readonly
 * @enum {String}
 */
export const Event = {
    START: 'wc-start', // Operation is started
    END: 'wc-end', // Operation is completed
    OBJECT: 'wc-object', // Object event
    TEXT: 'wc-text', // Text event
    BINARY: 'wc-binary', // Binary event
    ERROR: 'wc-error', // Error event
};
