name: Build go-server images (manual)
on:
  workflow_dispatch
jobs:
  build:  
    strategy:
      matrix:
        arch: [arm, arm64, amd64]
    runs-on: ${{ matrix.arch == 'amd64' && 'ubuntu-latest' || matrix.arch }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set variables
        id: var
        run: |
          echo "arch=${{ matrix.arch }}" >> $GITHUB_OUTPUT
          echo "tag=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          echo "image=ghcr.io/${{ github.repository_owner }}/go-server-${{ matrix.arch }}" >> $GITHUB_OUTPUT
          echo "nginx=1.23.2" >> $GITHUB_OUTPUT
          echo "platform=linux" >> $GITHUB_OUTPUT
      - name: Login
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build
        run: |
          docker build \
            --tag ${{ steps.var.outputs.image }}:${{ steps.var.outputs.tag }} \
            --build-arg VERSION=${{ steps.var.outputs.nginx }} \
            --build-arg ARCH=${{ steps.var.outputs.arch }} \
            --build-arg PLATFORM=${{ steps.var.outputs.platform }} \
            -f etc/docker/Dockerfile .
      - name: Push
        run: |
          docker push ${{ steps.var.outputs.image }}:${{ steps.var.outputs.tag }}
