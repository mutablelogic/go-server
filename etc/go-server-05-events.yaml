
# Plugins to load, the order of these is important
plugins:
  - build/events.plugin
  - build/log.plugin
  - build/basicauth.plugin

# Handlers for serving plugins  
handlers:
  events:
    prefix: /api/events
    middleware:
      - log
      - basicauth
