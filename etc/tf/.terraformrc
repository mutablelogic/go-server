# Dev override — point Terraform at the locally-built provider binary.
# Copy or symlink this file to ~/.terraformrc (or set TF_CLI_CONFIG_FILE)
# before running terraform plan/apply.
#
#   export TF_CLI_CONFIG_FILE=$PWD/.terraformrc
#
provider_installation {
  dev_overrides {
    "mutablelogic/kaiak" = "/Users/djt/go/bin"
  }
  direct {}
}
