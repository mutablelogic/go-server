
# Load the static site serving plugin
plugins:
  - build/static.plugin
  - build/log.plugin

# Handlers for serving plugins  
handlers:
  static:
    prefix: /api/static
    middleware:
      - log

# Where to serve static files from
static:
  "/": etc/docs
