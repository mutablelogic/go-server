
# Load the logging plugin
plugins:
  - build/log.plugin

log:
  # Include a prefix to each log line
  prefix:
  # Add information to the log:
  flags:
    #- default
    #- date
    #- time
    #- microseconds
    #- utc
    #- msgprefix
