
# Load the static site serving plugin
plugins:
  - build/static.plugin
  - build/log.plugin

# Handlers for serving plugins  
handlers:
  static:
    prefix: /api/static
    middleware:
      - log

# Add additional static handling here
static:
  "/dev": "${HOME}/Projects/js-components/dist"
